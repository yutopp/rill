#if 0
#include <string.h>
#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>

uint32_t rill_strlen(char const* s)
{
    return strlen(s);
}

int32_t rill_strcmp(char const* a, char const* b)
{
    return strcmp(a, b);
}
#endif
