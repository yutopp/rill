def main(): int
{
    val i = 10;
    val j: mutable(int) = 20;
    ref k = i;
    val arr = [i, j, k];

    val ii:mutable(int) = 0;
    while( ii < 3 ) {
        p(arr[ii]);
        ii = ii + 1;
    }

    return 0;
}