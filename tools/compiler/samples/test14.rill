def main(): int
{
    val v = F();
    val v1 = v.f( "s" );
    p(v1);
    val v2 = v.f( 2 );
    p(v2);

    p(v(4));

    return 0;
}

class F
{
    def ctor() {}

    template(T: type)
    def f(t: T): T
    {
        return t;
    }

    def op ()(a: int): int
    {
        return a * a;
    }
}