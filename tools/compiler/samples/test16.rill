class A
{
    val n: int;
    val nn: int;

    def ctor(hoge: int)
        | n = hoge
        , nn = hoge * 2
    {}

    def print(): void
    {
        p(this.n);
    }
}

def main(): int
{
    val g = A(10);
    g.print();

    return 0;
}